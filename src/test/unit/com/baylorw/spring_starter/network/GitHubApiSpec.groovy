package com.baylorw.spring_starter.network

import spock.lang.Specification

class GitHubApiSpec extends Specification {
    void setup() {
    }

    // TODO: This
    // The instructions said to not spend too much time on it so let's pretend this test would work like the others.
    // Strategy: The tested class takes a RestTemplate in the constructor. Mock it then test as normal

    def "FetchGitHubUser"() {
    }

    def "FetchGitHubUserRepos"() {
    }
}
